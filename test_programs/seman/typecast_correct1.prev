fun wrapper(): int = {

	(1 : int) +
	('A' : int);

  a_int = (charptr : ptr int);
  a_char = (charptr : ptr char);
  a_bool = (charptr : ptr bool);
  record = (charptr : ptr rec(a: int));
  array = (charptr : ptr arr[5] int);

  : 1

  where
    var a_int: ptr int;
    var a_char: ptr char;
    var a_bool: ptr bool;
    var record: ptr rec(a: int);
    var array: ptr arr[5] int;

    var charptr: ptr char;
};