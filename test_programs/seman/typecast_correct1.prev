fun wrapper(): int = {
  
	(1 : int) +
	('A' : int);

  a_int = (voidptr : ptr int);
  a_char = (voidptr : ptr char);
  a_bool = (voidptr : ptr bool);
  record = (voidptr : ptr rec(a: int));
  array = (voidptr : ptr arr[5] int);

  : 1

  where
    var a_int: ptr int;
    var a_char: ptr char;
    var a_bool: ptr bool;
    var record: ptr rec(a: int);
    var array: ptr arr[5] int;

    var voidptr: ptr void;
};